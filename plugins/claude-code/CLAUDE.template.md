The obsidian-brain MCP server is available. Use it as the local-first source of operational memory.

- Search before creating new knowledge: `vault_search` or `semantic_search`.
- Inspect full context with `get_note_context`.
- Use `brain_health_check` at the start of a fresh session.
- Use `build_capture_review` and `build_evidence_dashboard` to review generated surfaces.
- Keep risky maintenance dry-run-first.
